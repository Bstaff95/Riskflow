# Riskflow Observation Wiki

This folder is the Obsidian synthesis layer for indicator observations.

Generated case notes live in `cases/`, reusable pattern notes live in `patterns/`, concept notes live in `concepts/`, and high-level maps live in `maps/`.

The wiki helps connect observations, but the structured records in `research/observations/` remain the evidence source.
